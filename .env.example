# Supabase (Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://yyxvyanwrlczyelbihui.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Simple site gate (exposed to the browser bundle — not real security)
NEXT_PUBLIC_SITE_PASSWORD=change-me

# Optional: cache duration for link preview refetch (days)
LINK_PREVIEW_CACHE_DAYS=7
